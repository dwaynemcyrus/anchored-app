# Synthetic test vault

This directory contains fictional Markdown used only by automated Rust tests
and optional disposable manual checks. It contains no personal vault content.

The fixture exercises nested notes, aliases, front-matter wikilinks, body
wikilinks, an unresolved placeholder, duplicate filenames, and an empty note.
Do not use it as a real vault or as an example of personal writing.
